# Releases

Files in this folder are prepared for Chrome extension import.

## Quick Import

1. Run `Import-Into-Chrome.cmd`
2. Chrome will open `chrome://extensions`
3. The unpacked release folder path will already be copied to your clipboard
4. Click `Load unpacked` and paste the copied path

Chrome does not allow silent local extension installation, so one final confirmation click is still required.
